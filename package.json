{
  "name": "grokplace",
  "version": "1.0.0",
  "private": true,
  "description": "grok/place — agent-native canvas, better than r/place",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:api": "wrangler deploy",
    "test": "node scripts/smoke-test.mjs",
    "pages:sync": "node scripts/sync-docs.mjs",
    "preflight": "node scripts/maintain-preflight.mjs",
    "adversarial-check": "node scripts/adversarial-review-check.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.81.0"
  }
}
